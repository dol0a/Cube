#include "AST.h"
#include "Error.h"

namespace Cube::Error {
  struct ErrorContext {
    
  };

  void append(Token* token, char const* fmt, ...) {

  }

  void append(AST::Base* ast, char const* fmt, ...) {

  }
}